<p><%= @task.title%></p>
<p><%= @task.details%></p>

<li><%= link_to "Back to list", tasks_path(@task) %></li>
<li><%= link_to "Edit your list", edit_task_path(@task) %></li>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
